meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{base_url}}/logout
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
