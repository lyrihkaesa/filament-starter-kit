meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/users/{{created_user_id}}
  body: none
  auth: inherit
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
